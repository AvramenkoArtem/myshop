<tr>
  <td><%= line_item.quantity %> &times; <%= line_item.product.title %></td>
  <td><%= button_to 'Delete item', line_item, method: :delete,
                    data: { confirm: 'Are you sure?' } %></td>
</tr>